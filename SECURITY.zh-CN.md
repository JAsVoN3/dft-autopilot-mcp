# 安全策略

[English](SECURITY.md) | **简体中文**

## DFT AutoPilot 如何处理凭据与算力

- 服务器**全部**凭据均从环境变量（或你客户端的安全配置 / 操作系统钥匙串）读取。它**不内置**任何属于自己的账号、密钥、IP 或算力额度。
- 它**只**连接**你**配置的算力后端，使用**你的**凭据，以**你的**账号身份执行。
- 维护者不运营任何托管服务，永远不会收到你的密钥、作业或数据。

## 安装即意味着你信任它做这些事

安装本 MCP 服务器 / 插件，等于允许一个 AI 客户端：

- 在你配置的后端上运行 shell 命令（`local` 跑在**本机**；`slurm`/`scnet` 以**你的**账号跑在**你的**集群上）；
- 读写所配置的工作区以及远程作业目录中的文件；
- 提交、监控、取消 HPC 作业，而这些作业会**消耗你的算力额度，并可能产生费用**。

请只配置你信任该客户端去操作的后端与目录，并审查源码。本软件**不附带任何担保**；见 README 中的免责声明与 Apache-2.0 LICENSE（第 7–8 条）。

## 加固建议

- 为 `SLURM_HOST` 优先使用一把**专用 SSH 密钥**，配一个权限受限的远程账号；provider 以 `BatchMode` 运行 SSH（不走交互式密码）。
- 把 HPC 凭据的权限收敛到最小所需；一旦泄露立即轮换。
- 别把真实的 `.env` 纳入版本控制（它已被 git 忽略；只有 `.env.example` 会随包发布）。
- 审批前先看作业方案——`dft-compute` 技能默认就要求"先方案后计算"。

## 报告漏洞

请通过 GitHub Security Advisories（仓库 Security 页的 "Report a vulnerability"）**私下**报告安全问题，而不要发公开 issue。请附上复现步骤与影响范围。我们力争在几天内回复确认。

Agent 技能与 MCP 工具的执行，可能不在你 AI 客户端的数据留存保证范围内——除非你的客户端与后端满足你的数据处理要求，否则请勿在敏感/机密输入上运行本工具。
